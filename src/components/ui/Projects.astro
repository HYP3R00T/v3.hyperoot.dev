---
import ProjCard from "@/components/base/ProjCard.astro";

import { projects } from "config";
---

<section id="projects" class="scroll-my-8 flex flex-col gap-4">
  <h1 class="pl-4 pt-8">Projects</h1>
  {
    projects.map((data) => {
      return <ProjCard {data} />;
    })
  }
</section>
